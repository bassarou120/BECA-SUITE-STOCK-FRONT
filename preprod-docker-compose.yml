version: '3.8'

services:
  front_beca_ml-app_v2:
    build:
      context: ./
      dockerfile: ./deploy_preprod/Dockerfile

#    image: front-lnb-stock:latest
    container_name: front_beca_ml_v2
    restart: always

    ports:
      - 8089:80
#    networks:
#      - web
#
#
networks:
  default:
    external : true
    name: lnb-network
