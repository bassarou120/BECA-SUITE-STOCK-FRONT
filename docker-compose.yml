version: '3.8'

services:
  front_beca_stock-app:
    build: .

    #    image: front-lnb-stock:latest
    container_name: front_beca_stock
    restart: always

    ports:
      - 8082:80
#    networks:
#      - web
#
#
networks:
  default:
    external : true
    name: arcep-network
